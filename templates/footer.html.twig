<footer class="footer mt-auto py-3 ">
  <div class="">
    <span class="text-light">    
      <ul class="nav justify-content-center border-bottom pb-3 mb-3">
        <li class="nav-item"><a href="{{path('mesInfos')}}" class="nav-link px-2 text-muted">User</a></li>
        <li class="nav-item"><a href="{{path('modifier')}}" class="nav-link px-2 text-muted">Groupes</a></li>
        <li class="nav-item"><a href="{{ path('trombinoscope') }}" class="nav-link px-2 text-muted">Outils</a></li>
        <li class="nav-item"><a href="{{ path('stockage') }}" class="nav-link px-2 text-muted">Ressources</a></li>
        {% if is_granted('ROLE_RH')%}
          <li class="nav-item"><a href="{{ path('listeDepartements') }}" class="nav-link px-2 text-muted">R.H.</a></li>
        {% endif %}
        {% if is_granted('ROLE_ADMIN')%}
          <li class="nav-item"><a href="{{ path('changerMotDePasseAdmin') }}" class="nav-link px-2 text-muted">Admin</a></li>
        {% endif %}
      </ul>
        <p class="text-center text-muted">&copy; 2024 IPHC IN2P3 CNRS</p>
    </span>
  </div>
</footer>