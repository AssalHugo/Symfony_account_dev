{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1 class="text-center">Identifiants numériques</h1>
        <br>
        {{form(formIdentifiants)}}
        <h5 class="text-left">Test publications : </h5>
        {{form(formTest)}}
        <div class="overflow-scroll">
            {{content | raw}}
        </div>
    </div>    
{% endblock %}